# NeoGiga backend — environment template.
# Copy to .env, then run: php artisan key:generate
# NEVER commit .env. See DEPLOYMENT_NOTES.md for production hardening.

APP_NAME=NeoGiga
APP_ENV=local
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=debug

# --- Database -----------------------------------------------------------
# Blueprint mandates PostgreSQL 16+ (Blueprint §10). For quick local dev you
# may use sqlite: set DB_CONNECTION=sqlite and create database/database.sqlite
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=neogiga
DB_USERNAME=neogiga
DB_PASSWORD=

# --- Cache / session / queue --------------------------------------------
# Production: redis for all three (Blueprint §41 cache hierarchy).
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_SECURE_COOKIE=false   # true in production (HTTPS only)
CACHE_STORE=database
QUEUE_CONNECTION=database
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# --- Mail ----------------------------------------------------------------
MAIL_MAILER=log
MAIL_FROM_ADDRESS="hello@neogiga.com"
MAIL_FROM_NAME="${APP_NAME}"

# --- NeoGiga platform -----------------------------------------------------
# Interim admin API gate. Generate with: php -r "echo bin2hex(random_bytes(32));"
# Leave empty to fail closed (all /api/admin/* requests refused).
ADMIN_API_TOKEN=

# Marketplace domain hints (used by seeders/config; resolution is DB-driven).
NEOGIGA_GLOBAL_DOMAIN=neogiga.com
NEOGIGA_INDIA_DOMAIN=neogiga.in
NEOGIGA_NEPAL_DOMAIN=giganepal.com

# --- Supplier catalogue ingestion (disabled until source-policy review) ---
CATALOG_IMPORT_ENABLED=false
CATALOG_IMPORT_USER_AGENT="NeoGigaCatalogBot/1.0 (+catalog-admin-contact)"
CATALOG_IMPORT_CONTACT=
CATALOG_IMPORT_DEFAULT_RPM=10
CATALOG_IMPORT_MAX_CONCURRENCY=1
CATALOG_IMPORT_MEDIA_ENABLED=false
CATALOG_IMPORT_DOCUMENTS_ENABLED=false
CATALOG_IMPORT_TIMEOUT=30
CATALOG_IMPORT_CONNECT_TIMEOUT=10
CATALOG_IMPORT_MAX_RESPONSE_MB=10
CATALOG_IMPORT_RETRY_ATTEMPTS=3
CATALOG_IMPORT_REVIEW_STATUS=pending_review
ADAFRUIT_IMPORT_ENABLED=false
ADAFRUIT_IMPORT_RPM=
ADAFRUIT_MEDIA_ENABLED=false
WAVESHARE_IMPORT_ENABLED=false
WAVESHARE_IMPORT_RPM=
WAVESHARE_MEDIA_ENABLED=false
OKYSTAR_IMPORT_ENABLED=false
OKYSTAR_IMPORT_RPM=
OKYSTAR_MEDIA_ENABLED=false

# --- AI (Phase 2+; leave unset — no paid API is called when absent) --------
# ANTHROPIC_API_KEY=
# AI_MODEL=claude-sonnet-5
